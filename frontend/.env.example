# =============================================================================
# ASK Frontend — Environment Variables
# Copy this file to .env and adjust if needed.
# NEVER commit .env to version control.
# =============================================================================

# API base URL (defaults to /api which uses Vite proxy in development)
VITE_API_BASE_URL=/api
